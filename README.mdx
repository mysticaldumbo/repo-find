# repo find
python script to fetch a random public repo from github and opn it in the default browser.

## install
```batch
git clone https://github.com/mysticaldumbo/repo-find.git
pip install requests
```

## usage
```batch
cd tests
python main.py
```
*bruh it finds like really old repos only and i'm not gonna fix it 😭😭😭😭*
